name: three-jobs-in-the-workflow
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: i want ot build
        run: echo "building"
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: testing
        run: echo "testing it"

  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploying
        run: echo "runing the deploy"
